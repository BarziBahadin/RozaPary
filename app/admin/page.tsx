import { HostStudio } from '@/components/host-studio';
export const metadata = {
  title: 'Host studio | Roza',
  robots: { index: false, follow: false },
};
export default function AdminPage() {
  return <HostStudio />;
}
